# frozen_string_literal: true

RSpec.describe Mapp2g do
  it "has a version number" do
    expect(Mapp2g::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
